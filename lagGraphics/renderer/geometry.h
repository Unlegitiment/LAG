#pragma once
#include <vector>
//@TODO: Add the functionality to this class to allow for the dynamic vertex stuff to occur. 
class Geometry {
//public:
//    void Init(std::vector<Vertex>& crossbowmynuts, std::vector<uint32_t> indices) {
//        this->m_pVertexBuffer->Init(crossbowmynuts);
//        this->m_pIndexBuffer->Init(indices);
//    }
//private:
//    grcVertexBuffer<Vertex>* m_pVertexBuffer; // @TODO: diff the memory pattern from the memory itself :)
//    grcIndexBuffer* m_pIndexBuffer;
};